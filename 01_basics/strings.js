// template literals 

// let specailsting = `this is the way of write s template literals ${}`;

// escape character \n is used to change the line

// open tab \t

// strings method 
//these are built in functions to manipulate a string 
// str.toUpperCase() change the lower case to upper case return a new value
// str.toLowerCase()
// str.trim() removes white space fro the start and end 
// str.slice(start, end?) 
// str.concat(str)
// str. replace(searchVal, newVal)
// str.charAt(idx)









